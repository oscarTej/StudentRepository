package edu.iesam.studentplayground.students.Domain


// automaticamente ponemos los atributos en el constructor que es este por defecto
class Student (val exp: String, val name : String) {

    // diferencias entre var y val: val es inmutable (no hay set)

}